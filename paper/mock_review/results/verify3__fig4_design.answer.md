Resolved from round 1

- Bearing-load wording is now aligned between caption and body. The revised caption says panel (b) “couples impacts into the powder column without loading the auger bearing” (`caption_fig4_design.md`). The body now says the tap collar “couples solenoid impacts into the powder column without loading the auger bearing” (`main.pdf`, p. 4 / Fig. 2 discussion). This addresses the round-1 request to standardize bearing wording.
- The strongest overclaim in panel (b) was softened, but not fully fixed. Round 1 asked to replace wording like “carrying the solenoid striker and ERM vibration motor” with wording that the geometry is designed to mount them. The revised caption now says “Split-clamp tap collar designed to carry the solenoid striker and ERM vibration motor” (`caption_fig4_design.md`), and the panel title is “Tap collar + solenoid mount (split clamp)” in `make_figures.py::fig4()`. That is better than an unqualified “carrying” claim, because it now signals design intent rather than visible inclusion.
- The revised figure is internally consistent about the basic panel identities. Panel (a) is still explicitly the auger cross-section and panel (b) the tap-collar detail in all three places: the exported PDF (`fig4_design.pdf` text extraction: “Auger tube cross-section”; “Tap collar + solenoid mount (split clamp)”), the caption (`caption_fig4_design.md`), and the generator (`make_figures.py::fig4()`).
- The hopper/load-cell confusion that affected other figures is not present here. Nothing in this figure or caption now claims a separate hopper or an HX711/load-cell sensor. That is consistent with the corrected manuscript statements that “there is no separate hopper” and sensing is by an “A&D HR-100A analytical balance” (`main.pdf`, p. 2; `pr97_comments.md` lines 16–21, 125–127).

Previously flagged issues not yet resolved

- Panel (b) still does not show the actuators that the caption names. The caption still claims the split-clamp tap collar is “designed to carry the solenoid striker and ERM vibration motor” (`caption_fig4_design.md`), but the bundled asset `assets/tap_collar_final_iso.png` shows only the collar and a gray mounting block. No solenoid striker or ERM vibration motor is visible in the render or in the exported PDF (`fig4_design.pdf`). So the round-1 issue is only partially resolved.
- Panel (a) still claims “flight clearances” without showing or annotating any clearance. The caption remains “Cross-section of the printed auger tube showing the helical channel and flight clearances” (`caption_fig4_design.md`), but panel (a) in `assets/auger_geared_cross_section.png` / `fig4_design.pdf` has no arrows, dimensions, inset, or label marking a clearance. This specific round-1 issue remains open.
- Panel (b) still is not named as honestly or specifically as the visible geometry warrants. The visible asset appears to be a tap-collar sub-assembly with a mount block, not the full actuator arrangement. The branch tree names likely provenance files under `origin/copilot/design-tap-collar`, including `design/cad/tap-collar/renders/assembly_iso.png`, `assembly_side.png`, `tap_collar_iso.png`, and `mount_plate_iso.png` (`all_branches_file_tree.txt`). The current caption does not mention the mount plate/sub-assembly even though the gray block is visible in the asset.
- The figure still lacks the dimensions/callouts requested in round 1. In `make_figures.py::fig4()` the panels are placed as raw images with only panel letters and short titles; there are no feature arrows, dimensions, scale bars, or in-panel labels. The round-1 reviewer request for auger OD, internal diameter(s), pitch/lead, and clearance on panel (a), plus actuator/load-path callouts on panel (b), is not implemented.
- The layout/readability problem is still basically unchanged. `make_figures.py::fig4()` still uses a narrow single-column figure (`figsize=(SINGLE_COL_IN, 3.4)`) and a tall full-length panel (a) with width ratio `0.62`, so the revised figure still presents “design specifics” mostly as unlabeled renders rather than readable specifics.
- The asset-duplication issue remains. `make_figures.py` uses `tap_collar_final_iso.png` in both `fig2()` and `fig4()`. In `fig2()` it is panel (b), “Tap collar after review iterations (final part redesigned in Zoo)”; in `fig4()` it is panel (b), “Tap collar + solenoid mount (split clamp).” So the design-specifics figure still reuses the same tap-collar render already used in the AI-outcomes figure.
- The provenance/signposting issue remains. The manuscript text says the production tap collar was “subsequently redesigned in Zoo Design Studio” and names the Zookeeper agent in the broader workflow (`main.pdf`, pp. 4–6; `pr97_comments.md` lines 63–65, 128–130), but Fig. 4’s caption has no HUMAN-vs-AI provenance cue and does not state that no GUI CAD package was used. Round 1 treated this as optional/lower priority, but it is still unresolved.
- Bundle/manuscript numbering is still inconsistent. The bundle labels this artifact as “fig4_design” / “Figure 4 (design specifics)” (`README.md`, `caption_fig4_design.md`), but in the compiled manuscript the same content is Fig. 2 and is cited as Fig. 2 in the body (`main.pdf`, p. 4: “shown in Fig. 2”).

Remaining concrete inconsistencies or unsupported claims that can be fixed without new bench data

- Caption-to-render mismatch in panel (b): visible hardware vs named hardware. The render shows the collar plus a gray mount block, not the solenoid striker or ERM motor. The clean fix is to rewrite panel (b) to something like “Split-clamp tap-collar sub-assembly with mount plate, designed to mount the solenoid striker and ERM vibration motor,” unless you swap in a richer render from branch-tree candidates such as `design/cad/tap-collar/renders/assembly_side.png` or `assembly_iso.png` on `origin/copilot/design-tap-collar`.
- Unsupported “flight clearances” claim in panel (a). As drawn, panel (a) supports “helical channel” but not “flight clearances.” Either remove “flight clearances” from `caption_fig4_design.md`, or add an inset/arrow/dimension in `make_figures.py`. The branch tree suggests a potentially clearer source asset, `cad/auger-geared/archimedes-auger-geared-short-cross-section.png` on `origin/copilot/add-new-auger-design`.
- Undocumented visible subpart in panel (b). The gray lower component is visible but unnamed in both the title and caption. The branch tree suggests it corresponds to the tap-collar mount plate: `design/cad/tap-collar/mount_plate.step`, `stl/mount_plate.stl`, and `renders/mount_plate_*.png` on `origin/copilot/design-tap-collar`. The caption should name it if that is what is shown.
- “Design specifics” is still weakly signposted because neither panel has callouts. This is not a style nit. Without labels, the figure does not actually point out the design specifics claimed in text: helical channel, clearance, split clamp, strike path, ERM location, and the avoided bearing-load path. All of those can be added in `make_figures.py` without new experiments.
- Panel (a) likely shows a feature that is not explained. The cross-section includes two transverse protrusions/slots near the lower part of the tube, visible in `assets/auger_geared_cross_section.png`, but the caption does not mention them. They may be the loading slots described in the body (“loaded through slots in the auger tube,” `main.pdf`, p. 2), but that is not stated here. At minimum, add a label if they are intended to be the slots.
- Figure-level provenance is still underspecified relative to the manuscript’s own standards. The manuscript is explicit that the human team made design decisions and reviewed outputs, AI tools modelled the CAD, no Fusion 360/SolidWorks was used, and Zoo Design Studio/Zookeeper entered late (`main.pdf`, pp. 1–2, 4–6; `pr97_comments.md` lines 22–36, 128–130). Fig. 4 can reflect that with one short caption clause, especially because panel (b) is tied to the late-stage Zoo redesign in the main text.
- The duplicated tap-collar render weakens the logic of the figure set. Because `tap_collar_final_iso.png` already appears in `fig2()` as the workflow-outcome panel, its reuse here adds little new information. If the figure stays two-panel, panel (b) should preferably be replaced by a different view from branch-tree paths such as `design/cad/tap-collar/renders/assembly_side.png`, `assembly_top.png`, or `tap_collar_side.png` (`origin/copilot/design-tap-collar`).

Short prioritized action list

- Rewrite panel (b) caption/title to match the visible asset: say sub-assembly or mount plate, and say “designed to mount” rather than implying the solenoid/ERM are shown.
- Remove “flight clearances” from panel (a) caption unless you add a labeled inset or dimensions for the clearance.
- Add direct callouts in `make_figures.py` for the actual claimed specifics: helical channel, loading slot(s) if intended, split clamp, mount plate, and the intended actuator locations/load path.
- Replace the reused `tap_collar_final_iso.png` with a more informative alternate view from `origin/copilot/design-tap-collar` if available, especially `design/cad/tap-collar/renders/assembly_side.png` or `assembly_iso.png`.
- Add one short provenance clause for HUMAN vs AI roles consistent with the manuscript, and keep the no-GUI-CAD statement consistent with `main.pdf` / `pr97_comments.md`.
- Reconcile bundle naming with compiled manuscript numbering so “fig4_design” vs “Fig. 2” does not keep generating review noise.

- Discretionary analytical decisions made during the analysis
- Treated the compiled `main.pdf` as the authority for what the revised manuscript currently says about this figure, while still reviewing the supplied artifact under its bundle name `fig4_design`.
- Counted the panel (b) wording change from an implied present-tense hardware claim to a design-intent claim (“designed to carry”) as a partial resolution, not a full one, because the actuators remain absent from the render.
- Used visible content of the bundled raster assets (`assets/auger_geared_cross_section.png`, `assets/tap_collar_final_iso.png`) as primary evidence alongside the caption, PDF export, and generator script.
- Used `all_branches_file_tree.txt` only to name plausible replacement or provenance files, without asserting their unseen contents beyond what the filenames support.
- Limited attribution to HUMAN vs AI and kept the no-GUI-CAD point aligned with `main.pdf` and `pr97_comments.md`, per instruction.