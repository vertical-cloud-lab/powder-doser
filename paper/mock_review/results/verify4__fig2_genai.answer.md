Previously flagged issues now resolved

- AI vs HUMAN attribution is now explicit in the caption, and it matches the PR guidance. `caption_fig2_genai.md` now states: “All parts here were modelled by AI coding agents (programmatic CAD) and, for the production tap collar, the chat-driven Zoo Design Studio (Zookeeper agent); humans set the requirements and reviewed each iteration. No GUI CAD package was used.” This directly addresses the review request in `pr97_comments.md` lines 22–36 and 63–65 to signpost AI vs HUMAN roles, mention Zookeeper, and state that no GUI CAD package was used.
- The specific round-1 Fig. 2a wording problem is fixed. In `pr97_comments.md` lines 38–45, the HUMAN review said panel (a) was wrong to invoke the “not connected to its own mounting plate” problem, which belonged to a different bracket failure. The revised caption no longer says that. It now describes panel (a) as “an amalgamation of separate functions with part-to-part interferences, incorrect tolerancing, no clearance for the solenoid and vibration motor it must carry, and a general lack of spatial reasoning,” in `caption_fig2_genai.md` and identically in `main.pdf` Fig. 3 caption. That matches the HUMAN correction.
- The Zoo / Zookeeper provenance is now named in the figure caption, not just the body text. The current caption explicitly says the production tap collar was redesigned in “Zoo Design Studio” and later names the “Zookeeper agent” (`caption_fig2_genai.md`; `main.pdf` Fig. 3 caption). This resolves the round-1 request to mention Zookeeper specifically.
- The earlier body/caption conflict about a “raised-bracket solution” appears resolved by deletion. In the supplied revised `main.pdf`, I do not find the earlier “raised-bracket” framing; the only surviving account for panels (e–h) is the caption text about unexplained hole → raised platforms → gap → clean plate. So the specific wording conflict noted in `round1_CONSOLIDATED.md` under `fig2_genai` narrative is no longer present in the revised manuscript text.
- The earlier “v1 module” / “v2 module” wording issue is also effectively gone. In the supplied revised figure, panel (d) is titled only “Whole-assembly attempt (single prompt)” (`make_figures.py`, `fig2()`; extracted PDF text from `fig2_genai.pdf`). I do not find “v1 module” or “v2 module” wording in the revised `main.pdf`.

Previously flagged issues not yet resolved

- Figure-numbering metadata is still inconsistent. The bundle still calls this “Figure 2”: `README.md` line 3 says “Location in manuscript: main.tex, Figure 2 (generative-AI CAD outcomes)”, and `caption_fig2_genai.md` line 1 says “Caption for fig2_genai (main.tex, Figure 2 …)”. But the compiled manuscript labels the same figure as “Fig. 3 Generative-AI CAD outcomes, good and bad” (`main.pdf`, page 5 text extraction). This round-1 issue is not fixed.
- Panel (f) is still the odd one out and still appears to be the same mismatch flagged in round 1. `make_figures.py` still uses `plate_iter2_platforms_iso.png` for panel (f), while the surrounding sequence uses `plate_iter1_hole_top.png`, `plate_iter3_gap_top.png`, and `plate_iter4_final_top.png`. The filenames themselves show the framing inconsistency: three are `*_top.png`, but panel (f) is `*_iso.png`. In the rendered figure, (f) is a distant isometric scene with a table-like base, unlike the tighter plate views in (e), (g), and (h). That means the round-1 complaint in `round1_CONSOLIDATED.md` (“Replace panel (f) with the correct mounting-plate iteration render in the same framing as (e), (g), and (h)”) is still unresolved.
- Panel (d) is still not evidentially matched to its caption. The title and caption say this is a “Whole-assembly attempt (single prompt)” with “interferences and floating components” (`make_figures.py`; `caption_fig2_genai.md`; `main.pdf` Fig. 3 caption). But the asset used is `assets/single_channel_module_iso.png`, which corresponds in the branch tree to `design/cad/single-channel-module/renders/single_channel_module_iso.png` (`all_branches_file_tree.txt`). That source path reads like a generic single-channel-module render, not a clearly archived “single-prompt failure” artifact. More importantly, the defects claimed in the caption are not clearly visible in the panel without arrows or callouts. This round-1 issue is not fixed.
- Panel (b) ambiguity is reduced but not fully resolved. The caption now distinguishes the shown part from the later Zoo production redesign: “The same part after iterative review in the programmatic workflow; the production tap collar was subsequently redesigned in Zoo Design Studio …” (`caption_fig2_genai.md`). That is better. But the panel title generated by `make_figures.py` is still “Tap collar after review iterations (final part redesigned in Zoo)”. On the figure itself, a skimming reader could still read panel (b) as the final Zoo-derived part rather than the last programmatic iteration. So the round-1 request to resolve this ambiguity is only partially satisfied.
- The stale-input root-cause claim is still asserted without file-level support in the bundle. The caption still says: “The root cause was stale input files—a human-side error the agent silently designed around.” That claim appears in `caption_fig2_genai.md` and the compiled `main.pdf` Fig. 3 caption. But the local bundle does not include the underlying per-iteration CAD/code provenance needed to audit that causal claim. The branch tree does name relevant candidates such as `design/cad/mounting-plate-assembly/cadsmith_runs/mounting_plate/mounting_plate_iter0_script.py`, `.../mounting_plate_iter0_render.png`, and the manuscript assets `paper/figures/assets/plate_iter1_hole_top.png`, `plate_iter2_platforms_iso.png`, `plate_iter3_gap_top.png`, `plate_iter4_final_top.png` (`all_branches_file_tree.txt`), but the actual causal chain is not documented in the supplied files. This round-1 issue remains unresolved.
- The requested defect signposting for panels (e–h) is still missing. `make_figures.py` adds only panel labels and titles; there are no arrows, circles, zooms, or in-panel annotations for the unexplained hole, the raised-platform region, or the gap. The revised `fig2_genai.pdf` likewise shows no callouts. That round-1 issue is still unresolved.

Remaining concrete inconsistencies or unsupported claims that can be fixed without new bench data

- Same asset, two roles: `tap_collar_final_iso.png` is reused across figures with conflicting status. In `make_figures.py`, this same file is used in `fig2()` panel (b) and `fig4()` panel (b). In the generative-AI outcomes figure, the caption says panel (b) is the post-review programmatic version and that “the production tap collar was subsequently redesigned in Zoo Design Studio” (`caption_fig2_genai.md`). But in the design-specifics figure, the same image is used as “Tap collar split clamp (actuator mount)” and described in `main.pdf` Fig. 2 as the tap collar that carries the solenoid striker and ERM vibration motor. Those two uses imply different statuses for the same render. Either:
  - `tap_collar_final_iso.png` is the pre-Zoo programmatic part, in which case the design-specifics figure is overstating it as the final production-relevant collar, or
  - `tap_collar_final_iso.png` is the Zoo/final collar, in which case the Fig. 3 caption is wrong to describe it as merely the programmatic post-review version.
  This is the cleanest remaining figure-family inconsistency to fix.
- Panel (d) likely overclaims provenance as well as defect visibility. The branch-tree path `design/cad/single-channel-module/renders/single_channel_module_iso.png` suggests a standard render from the single-channel-module branch, not a specifically archived “whole-assembly single-prompt failure” file. If that asset really is the intended failure case, the figure should say so in the caption or SI and name the branch-tree path that supports it. If not, panel (d) needs either a different asset or softened wording.
- Panel (e) title says “hole under gear,” but the panel does not show the gear clearly enough for that wording to be self-evident. The issue may be real, but as rendered it is not obvious. This is fixable with a crop, arrow, or title rewrite.
- Panel (f) title says “raised platforms instead of hole,” but because the asset is a zoomed-out isometric scene, the “raised platforms” are not the first thing a reader sees. Again, this can be fixed with a crop/callout or a replacement render from the same viewpoint family as (e), (g), and (h).
- Panel (d) logic is weak even if technically not false. The top row reads bad → ambiguous → good → ambiguous: (a) failure, (b) improved collar, (c) success, (d) claimed failure. Without explicit overlays, the row does not visually teach the “whole-assembly bad / part-by-part good” contrast that the caption and Section 2.3 want. That is fixable in figure composition and labeling only; no new measurements needed.
- The caption’s stale-input sentence is too strong for the evidence shown in the figure bundle. “The root cause was stale input files” is a causal attribution, not just a description. Unless you cite the underlying provenance files in the caption or SI, a safer wording would be to report what was observed: that incorrect upstream part inputs were later identified and that supplying corrected files produced the clean iteration. The branch tree gives candidate files to cite, but the current bundle does not substantiate the stronger causal sentence by itself.

Prioritized action list of implementable non-bench-data fixes

1. Resolve the tap-collar identity problem across figures. Decide whether `paper/figures/assets/tap_collar_final_iso.png` is the pre-Zoo programmatic collar or the production/Zoo collar, then make Fig. 3(b), Fig. 2(b), and their captions all say the same thing.
2. Fix panel (f). Replace `paper/figures/assets/plate_iter2_platforms_iso.png` with a matching top/frontal render of the same iteration, or re-render all of (e–h) from one fixed viewpoint.
3. Fix panel (d) either by swapping in a clearly archived whole-assembly failure render or by softening the title/caption so they match what `single_channel_module_iso.png` actually shows.
4. Add explicit callouts or zooms for the claimed defects in panels (d) and (e–h): the floating component(s), the unexplained hole, the raised-platform region, and the gap.
5. Fix the figure-number drift in `README.md` and `caption_fig2_genai.md` so the bundle metadata matches compiled `main.pdf` Fig. 3.
6. Soften or source the stale-input sentence. Best fix: cite the exact upstream iteration files from the branch tree in SI. Minimal fix: rewrite the sentence to avoid unsupported causal certainty.
7. Tighten panel (b) title text so the panel itself cannot be misread as the final Zoo-derived production collar. For example, make the title explicitly “Programmatic post-review tap collar; production version later redone in Zoo.”

- Used the compiled `main.pdf` as the authority for manuscript figure numbering and final caption text when it conflicted with bundle-local labels.
- Treated `caption_fig2_genai.md`, `make_figures.py`, the exported `fig2_genai.pdf`, and the supplied raster assets as the primary evidence for whether round-1 figure-specific issues were fixed.
- Used `all_branches_file_tree.txt` only to name candidate provenance files and branch paths; I did not assume contents beyond what the filenames support.
- Counted an issue as unresolved when the revised wording improved the story but the rendered panel still allowed the original misreading.
- Focused only on implementable figure/caption/provenance fixes and excluded the known synthetic-dispensing-data issue, per instruction.