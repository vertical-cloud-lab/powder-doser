# Quilter.ai routed candidate boards — run 3, 2026-07-15 (provenance)

The `candidate_N/` folders are the **auto-placed-and-routed** boards returned
by [Quilter.ai](https://www.quilter.ai) when @lbwinters re-ran the
`test_module_unplaced` trio generated by
[`../build_starter_board.py`](../build_starter_board.py) at commit
`0683525` — the first upload carrying the **second dispensing-angle servo**
(`M4` on Pico GP2, net `SERVO_SIG2`; PR #66 dual-servo hinge / PR #100 rev C —
16 footprints / 175 pads / 23 nets, 110 × 110 mm outline) with the reviewed
Quilter settings (JLCPCB 4-layer *with power plane* JLC04161H-7628, 10 mil /
6 mil rules, per-net power currents, bypass-cap assignments). Source: PR
[#76](https://github.com/vertical-cloud-lab/powder-doser/pull/76), the
2026-07-15 comment with six
`Quilter_test_module_unplaced.kicad_pcb_Candidate_N.zip` attachments (all six
links were valid this run).

| folder | source attachment |
| --- | --- |
| `candidate_1/` | `Quilter_test_module_unplaced.kicad_pcb_Candidate_1.zip` |
| `candidate_2/` | `Quilter_test_module_unplaced.kicad_pcb_Candidate_2.zip` |
| `candidate_3/` | `Quilter_test_module_unplaced.kicad_pcb_Candidate_3.zip` |
| `candidate_4/` | `Quilter_test_module_unplaced.kicad_pcb_Candidate_4.zip` |
| `candidate_5/` | `Quilter_test_module_unplaced.kicad_pcb_Candidate_5.zip` |
| `candidate_6/` | `Quilter_test_module_unplaced.kicad_pcb_Candidate_6.zip` |

Each folder holds Quilter's `test_module_unplaced.kicad_pcb` (placed + routed,
4-layer `F.Cu / In1.Cu (GND plane) / In2.Cu / B.Cu`) and the companion
`test_module_unplaced.kicad_pro`, committed **verbatim** as Quilter emitted
them.

`candidate_2_fixed/` is the **one exception to verbatim** — it is the
recommended candidate 2 with note 25's pre-fab **In2 zone fix applied**:
[`fix_candidate2_in2_zone.py`](fix_candidate2_in2_zone.py) (re-runnable,
KiCad 7 `pcbnew`) reassigns the exported `<no net>` zone on `In2.Cu` to
`GND` and refills it, turning the dead copper into a second solid GND plane
and clearing all 152 clearance/hole_clearance DRC errors (unconnected
ratsnest stays 0). `candidate_2_fixed/fix_report.json` is the captured
before/after report and `candidate_2_fixed/in2_gnd_plane.{svg,png}` the
rendered In2 plane. **This is the board file to fabricate.** (Re-runs of
the fix script yield an electrically identical board, but `pcbnew`'s save
order isn't byte-stable — verify via `fix_report.json`, not file hashes.)

`candidates_run3_copper.png` is a labeled montage of the six candidates
(top + bottom copper, outline, silk), rendered with
`kicad-cli pcb export svg`.

[`analyze_quilter_candidates_run3.py`](analyze_quilter_candidates_run3.py) is
the read-only, re-runnable analysis (track length, vias, KiCad DRC bucketed by
type, connectivity, per-net trace widths, placement side, bypass-cap
proximity, the systemic `<no net>` zone on `In2.Cu`, and two run-3-specific
checks: an explicit **M4/`SERVO_SIG2` routed-connectivity verification** and a
**full parts/netlist audit** against the upload trio).
`analysis_summary_run3.json` is its captured output.

[`simulate_power_ngspice_run3.py`](simulate_power_ngspice_run3.py) is the
read-only, re-runnable **ngspice** power-integrity simulation (resistor-mesh
IR-drop `.op` solve of the routed +12 V / +5 V / +3V3 copper + a transient
decoupling `.tran` of the +12 V solenoid rail), with the run-2 mesh upgrades
(T-junction splitting, layer-aware clustering, `module_internal_bridges`
detection) and the +5 V worst-case load updated for **two** servo headers
(M3 + M4). `analysis_power_ngspice_run3.json` is its captured output.

Full write-up: [`../../25-quilter-run3-candidate-evaluation.md`](../../25-quilter-run3-candidate-evaluation.md).
